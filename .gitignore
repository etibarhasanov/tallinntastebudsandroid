# Gradle
.gradle/
build/
local.properties

# Signing. The release key is the one thing that must never be in here: lose it
# and no future build can update an installed app; leak it and anyone can.
keystore.properties
*.jks
*.keystore

# Android Studio / IntelliJ
.idea/
*.iml
captures/

# Build outputs
*.apk
*.aab
*.ap_
*.dex

# macOS
.DS_Store
